import { initializeKinesis } from "./index";

document.addEventListener("DOMContentLoaded", () => {
  initializeKinesis(); // Assuming this is the main function you want to test
  console.log("Kinesis.js has been initialized");
});
